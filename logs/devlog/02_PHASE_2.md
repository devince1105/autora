# 開發紀錄 02 — 階段 2 執行環境、任務與事件

- 期間：2026-09-17 ~ 2026-09-18（完成）
- 目標：真正的執行環境可以跑工作流程圖；每一步都產生真實事件並更新代理活動狀態；包含工具事件、審批、預算閘門與當機恢復。
- 驗收條件（`logs/3d-office/09_DEVELOPMENT_ROADMAP.md` 階段 2）：啟動 EchoWorkflow（A→B→C，三個不同角色）後，`trace(run)` 依序出現「思考 → 工作（工具呼叫與完成）→ 檢查 → 完成」，`agent_activity` 依序變化，且 B 在 A 完成前處於「等待上游」。
- 使用模型：T-201、T-203 ~ T-210、T-212 ~ T-215 為 Claude Opus 5；T-202、T-211 為 Claude Fable 5.1

## 進度總覽

| 任務 | 內容 | 狀態 |
|---|---|---|
| T-201 | 工作流程執行、任務、代理執行、執行步驟的資料表與生命週期狀態機 | ✅ |
| T-202 | 任務管理員（佇列與租約） | ✅（Fable 5.1） |
| T-203 | 工作流程圖實例化與下游解鎖 | ✅ |
| T-204 | 工具登錄表與工具事件 | ✅ |
| T-205 | 政策引擎 | ✅ |
| T-206 | 審批 | ✅ |
| T-207 | 模型閘道、路由與假模型供應者 | ✅ |
| T-208 | Anthropic 模型供應者 | ✅ |
| T-209 | 成本守門員 | ✅ |
| T-210 | 執行軌跡與物件儲存 | ✅（S3 延後，見下） |
| T-211 | 代理執行器 | ✅（Fable 5.1） |
| T-212 | 排程器 | ✅（不使用 APScheduler，見下） |
| T-213 | 工作程序主程式與 EchoWorkflow | ✅（事件分派器延後，見下） |
| T-214 | 啟動工作流程的 API | ✅ |
| T-215 | 當機恢復測試 | ✅ |

---

## T-201 · 工作執行資料表與生命週期（遷移 0004）

### 做了什麼
- 資料表（`db/models/tasks.py`）：
  - `workflow_runs`：一次工作流程範本的實例，屬於某個專案；
  - `tasks`：原子工作單位，含 `required_role`、`depends_on`、`attempt`/`max_attempts`、`budget_usd`、`progress`、`priority`、租約欄位；
  - `agent_runs`：一次代理執行，含輸入輸出、評估結果、下游交接、錯誤、成本與權杖用量；
  - `agent_steps`：執行中的每一步（思考、行動、觀察、評估、修補），完整內容放物件儲存（T-210）。
- `projects.created_by_run_id` 補上外鍵，指向提出該專案的代理執行（Phase 1 時資料表尚不存在）。
- 生命週期狀態機（`runtime/lifecycles.py`）：`TASK_FSM`、`AGENT_RUN_FSM`、`WORKFLOW_RUN_FSM`。後續的任務管理員、工作流程圖、審批與代理執行器都必須透過它們改變狀態，因此每次轉換都有驗證與稽核紀錄。

### 由資料庫保證的規則
| 規則 | 理由 |
|---|---|
| 任務「正在執行」若且唯若持有租約（`lease_owner` 與 `lease_until` 同時存在） | 任務管理員若忘了釋放或忘了取得租約，寫入時就會失敗，不會留下幽靈租約或無主的執行中任務 |
| 代理執行處於終止狀態若且唯若有 `finished_at` | 避免「看起來還在跑、其實已結束」的資料 |
| 每個（任務、嘗試次數）只能有一筆代理執行 | 重試不會產生重複執行 |
| `attempt` 介於 0 與 `max_attempts` 之間 | 重試上限由資料庫擋住 |
| `agent_steps` 只能新增 | 執行軌跡是稽核資料，不可改寫（沿用 0002 的觸發器函式） |
| 名稱格式檢查（任務名稱、角色、範本名稱） | 與事件、佈局使用的識別字一致 |

索引依實際查詢設計：
- 工作程序領取任務：`(company_id, priority, created_at) WHERE state = 'READY'`；
- 回收過期租約：`(lease_until) WHERE state = 'RUNNING'`。

### 狀態機設計決策
- **失敗重試不經過 FAILED**：可重試的失敗走「執行中 → 就緒」（嘗試次數由任務管理員遞增），FAILED 只代表最終失敗；租約過期也走「執行中 → 就緒」。
- **審批會釋放工作程序**：代理任務走「執行中 → 等待審批 → 就緒」，核准後由工作程序重新領取並恢復同一個代理執行；人工或服務節點則是「就緒 → 等待審批 → 成功」。
- **代理執行必須先評估才能完成**：「執行中 → 已完成」不允許，必須經過「評估中」。
- 以測試保證三個狀態機的結構性質：每個狀態都能從初始狀態到達；每個狀態都能到達終止狀態（不會卡住）；每個非終止狀態都能被取消或中止。

### 遇到的問題
- （我造成的）又使用了不存在的 `mapped_column(type_=...)` 與 `Base.registry.type_annotation_map` 寫法，匯入時即失敗，改為 `mapped_column(JSONB)`。
- （我造成的）步驟測試在唯一性檢查後回滾，連帶把先前新增的步驟也回滾，導致後續的「只能新增」檢查修改 0 列、觸發器沒有觸發而誤判。與階段 1 的同類問題相同；改為拆成獨立測試。**教訓：在同一測試中，任何 `session.rollback()` 之後都不能假設先前寫入的資料仍存在。**

### 驗證
- `make migrate`、`make db-check`（模型與遷移一致）、降版到 0003 再升版成功。
- `tests/db/test_task_models.py`、`tests/runtime/test_lifecycles.py`；全部 Python 測試 **173 個通過**（`AUTORA_REQUIRE_DB=1`）。
- import-linter 兩條邊界規則仍全數遵守；事件結構產生物未變。

---

## T-212 · 排程器（遷移 0005）

### 做了什麼
- `schedules` 資料表：cron 表達式、時區、處理器名稱、下次執行時間、租約、最近錯誤與連續失敗次數。
- `runtime/scheduler.py`：每次 tick 領取到期排程（`FOR UPDATE SKIP LOCKED` 加租約），在同一個交易中執行處理器、推進下次執行時間、寫入 `SCHEDULE_FIRED`、清除租約。

### 與設計不同：不使用 APScheduler
- 技術選型原本寫 APScheduler。但 `schedules` 資料表本身已保存下次執行時間與租約；APScheduler 的記憶體排程會變成第二份狀態，可能與資料表不一致。
- 改為直接輪詢資料表，機制與任務佇列相同：多個工作程序天然互斥，重啟不遺失，也不需要額外協調。cron 解析使用 `croniter`。已在 `platform/13` 技術選型表加註。

### 語意
- **同一時間點只觸發一次**：4 個工作程序同時 tick，處理器只被呼叫一次（有測試）。
- **處理器失敗**：處理器的寫入全部回滾，下次執行時間不推進；保留租約到「現在 + 重試間隔」，避免每次 tick 都重試；記錄錯誤訊息與連續失敗次數。
- **錯過的執行會合併**：停機三天後恢復，只補觸發一次，接著回到正常節奏，不會一次補跑三天份。
- **當機的工作程序**：租約過期後由其他工作程序接手。
- 時區以排程自己的時區計算（例如台北 06:00 = UTC 前一天 22:00，有測試）。

### 驗證
`tests/runtime/test_scheduler.py`：11 個通過。

---

## T-204 · 工具登錄表與工具事件

### 做了什麼
- `runtime/tools/registry.py`：以裝飾器註冊工具；工具是 `async fn(args: 模型, ctx) -> ToolResult`，註冊時宣告副作用等級（read / write / irreversible）、逾時與是否可重試。
- `invoke()`：驗證參數 → 寫入 `TOOL_CALLED` → 在逾時限制內執行 → 寫入 `TOOL_COMPLETED`（含產出物 `produced`、成本）或 `TOOL_FAILED`。每次呼叫必定成對。

### 關鍵決策：拆成三個交易
- `emit()` 會持有該公司的事件鎖直到提交。若 `TOOL_CALLED` 與一個 30 秒的網頁抓取在同一個交易，這段期間同公司所有事件寫入都會被卡住。
- 因此：
  1. `TOOL_CALLED` 單獨提交（可附帶 `before_call`，讓代理執行器把「活動狀態 = 工作中」與它一起提交）；
  2. 工具在第二個交易執行，**工具的領域寫入與 `TOOL_COMPLETED` 一起提交**（例如證據資料列與事件要嘛都存在，要嘛都不存在）；
  3. 失敗時回滾第二個交易，另外提交 `TOOL_FAILED`。
- 有測試證明：工具執行期間，其他寫入者可以為同一公司寫入事件。

### 其他
- 參數驗證失敗、逾時、例外都會記錄為 `TOOL_FAILED`（先有 `TOOL_CALLED`），不會拋出；未註冊的工具會拋出 `UnknownTool`，因為沒有副作用等級可以記錄。
- 每次呼叫提供穩定的冪等鍵（範圍 + 步驟序號 + 工具名 + 參數雜湊），重試時不變，寫入型工具必須用它避免重複寫入。
- 政策判斷（允許 / 拒絕 / 需審批）不在登錄表內，由 T-205 政策引擎在呼叫前決定。

### 驗證
`tests/runtime/test_tools.py`：11 個通過。

---

## T-210 · 執行軌跡與物件儲存

### 做了什麼
- `infra/blobstore.py`：物件儲存介面與本機檔案實作。寫入為原子操作（暫存檔 + rename）；物件鍵只允許安全字元，不可能跳出根目錄。
- `runtime/trace/recorder.py`：`record_step()` 以代理執行資料列鎖定取得下一個序號，完整的提示與回應寫入物件儲存，資料表只存鍵。先寫物件再寫資料列：交易回滾只會留下無人引用的物件（無害），反過來則可能出現指向不存在物件的資料列。
- `runtime/trace/query.py`：`get_run_trace()` 回傳該執行的真實事件（依 `seq`），並以負載中的 `step_seq` 接上對應步驟；沒有事件引用的步驟也會列出。軌跡中沒有任何合成的資料。
- API：`GET /api/runs/{id}/trace`、`GET /api/runs/{id}/steps/{seq}/blob`（完整內容只從後者取得，有測試確認軌跡 API 不會洩漏提示原文）。

### 與設計不同：S3 延後
- 規格要求本機與 S3 兩種實作。目前沒有任何功能需要 S3，而 S3 的正確測試需要額外的 MinIO 服務。
- 已建立介面與共用的契約測試套件（`tests/infra/test_blobstore.py`）；之後加入 S3 時，只需讓它通過同一套測試。

### 遇到的問題
- （我造成的）把 API 測試放在 `tests/runtime`，但 `api` 測試夾具定義在 `tests/api/conftest.py`，因此找不到；一度用 `api._transport.app` 這種取巧寫法。改為把 API 測試移到 `tests/api/`，並由夾具正式覆寫物件儲存依賴。
- （我造成的）寫了一個沒有檢查任何東西的測試（`test_agent_step_model_is_imported`），已刪除。

### 驗證
物件儲存 9 個、軌跡 4 個、執行 API 3 個測試通過。

---

## T-207 · 模型閘道、路由與假模型供應者（遷移 0006）

### 做了什麼
- `runtime/models/types.py`：與供應商無關的請求與回應格式（文字、工具呼叫、工具結果區塊）。請求只指定「能力」（例如 `drafting`），不指定模型。
- `runtime/models/router.py`：（角色, 能力）→ 別名 → 綁定（供應商、模型 ID、價格）。查找順序由具體到一般；每個別名可設定備援鏈。價格以「每百萬權杖美元」計，成本精確到小數第 6 位。
- `runtime/models/providers/fake.py`：假模型供應者，依（角色, 任務名稱, 嘗試次數）依序回放腳本，可模擬結構化輸出、工具呼叫、延遲與錯誤。除了模型本身，閘道、成本紀錄、事件全部是真的。
- `runtime/models/gateway.py`：路由 → 成本預留 → 呼叫供應商 → 計算成本 → 寫入 `model_calls` 並在同一交易中結算預留 → 驗證結構化輸出。
- `model_calls` 資料表：每次呼叫一列（包括失敗），記錄公司、專案、任務、執行、代理的歸屬與權杖用量；只能新增。這是模型成本的唯一真相來源。

### 設計決策
- **結構化輸出不合格不是錯誤**：以 `output_issues` 回傳，交給代理的修補步驟處理；也接受包在 ```json 區塊中的輸出。
- **備援只用於供應商問題**（服務中斷、過載），不會因為預算不足或輸出不合格而切換模型。
- **閘道不寫事件**：模型呼叫是高頻的帳本資料，不是領域事實。
- 程式碼中不得出現任何模型 ID：有測試掃描整個套件，找不到 `claude-`、`gpt-`、`gemini-` 開頭的字串。
- `MODEL_PROVIDER=anthropic` 的路由設定屬於 T-208。

### 驗證
`tests/runtime/models/test_gateway.py`：16 個通過（包括備援時兩次呼叫都有紀錄、不可備援的錯誤直接失敗）。

---

## T-209 · 成本守門員（遷移 0007）

### 做了什麼
- `runtime/cost/guard.py`：每次模型呼叫前檢查四層預算，任何一層超出就拒絕，並寫入 `BUDGET_EXHAUSTED` 事件（即使呼叫沒有發生，事件也會提交）。

| 層級 | 上限來源 | 計入的花費 |
|---|---|---|
| 執行 | 代理的 `budget.per_run_usd` | 此執行的模型呼叫 |
| 任務 | `tasks.budget_usd` | 此任務所有執行的模型呼叫 |
| 專案 | 該專案的硬上限預算（日 / 月） | 該專案在期間內的呼叫 |
| 公司 | 沒有指定專案的硬上限預算 | 全公司在期間內的呼叫 |

- 花費 = 已記錄的 `model_calls` 成本 + 進行中呼叫的預留金額（`cost_reservations` 資料表）。
- 預留在每間公司一把的交易諮詢鎖內完成：**5 個並行請求搶只夠一次的預算，恰好 1 個成功、4 個被拒**（有測試）。
- 預留超過 15 分鐘未結算視為過期，不再計入，避免當機的工作程序永久占用預算。
- 成功呼叫在寫入 `model_calls` 的同一交易中結算預留（連結呼叫、記錄實際成本）；失敗呼叫則釋放預留。

### 與設計不同或需要注意之處
- **`BUDGET_EXHAUSTED` 從公司層移到執行環境層**：由執行環境的成本守門員觸發，而執行環境不可匯入公司層（import-linter 會擋）。事件結構已重新產生，並回寫 `3d-office/03 §8`。
- **估算刻意偏高**：輸入權杖以「字元數 / 4」估算，輸出以完整的 `max_output_tokens` 計。
- **「週期」預算暫時以「日」計算**：週期資料表尚未建立，而 MVP 的週期就是每天。
- **軟上限（`hard_cap = false`）不在此擋下**，留給報表使用。
- **目前只計模型成本**：工具成本等工具用量寫入帳本後再納入。

### 遇到的問題
- （我造成的）在測試中再次使用 `__import__` 取巧寫法，已改為正常匯入。
- （我造成的）移除未使用欄位 `company_wide` 時，替換字串因格式化而對不上導致失敗，但串接的指令仍印出「已移除」。之後確認實際未移除並重新處理。**教訓：修改檔案的指令不要用 `;` 串接成功訊息，要以實際檢查結果為準。**

### 驗證
`tests/runtime/test_cost_guard.py`：12 個通過（包括被拒的呼叫完全不會到達模型供應者、也不會產生呼叫紀錄）。

---

## T-202 · 任務管理員（遷移 0008）

### 做了什麼
- `runtime/task_manager.py`：`tasks` 資料表就是佇列。工作程序以 `FOR UPDATE SKIP LOCKED` 領取任務並持有租約；每次狀態變更都經過 `TASK_FSM` / `AGENT_RUN_FSM`（有稽核）並在呼叫端的交易內發出事件。所有方法都不自行提交：任務變更、事件與代理活動狀態要嘛一起落地，要嘛全部不落地。
- 操作：`add_task`、`mark_ready`、`claim_next`、`heartbeat`、`succeed`、`fail`、`abort`（預算 / 政策 / 人為）、`cancel`、`release_blocked`、`reap_expired_leases`。
- 依賴傳播（哪些下游任務變為就緒、上游最終失敗時取消哪些下游）留給 T-203，透過 `on_task_finished` / `on_task_failed` 兩個掛鉤接入。

### 讓佇列誠實的規則
| 規則 | 做法 |
|---|---|
| **一次領取一個令牌** | 領取時產生令牌寫在任務上；租約被回收後，原工作程序再試圖完成任務會得到 `LeaseLost`，不會覆寫任何資料。只有回收器能收回租約 |
| **每個代理同時只有一個未完成的執行** | 資料庫部分唯一索引 `uq_agent_runs_open_per_agent`。代理是一個人，只能有一個目前狀態 |
| **領取即消耗一次嘗試** | 當機也算一次（有毒任務不能無限次弄垮工作程序）；預算受阻也算（該次執行的資料列已存在，嘗試編號無法重用），`release_blocked` 對用完嘗試次數的任務直接判定最終失敗 |
| **重試有退避** | 新欄位 `available_at` = 現在 + 10 秒 × 2^(嘗試次數−1)，上限 5 分鐘 |
| **FAILED 只代表最終失敗** | 可重試的失敗回到 READY；租約過期也回到 READY |
| **佇列以公司為範圍** | 領取查詢一律以代理所屬公司過濾（有測試：另一家公司的同角色代理領不到） |
| **等待審批的執行會被延續** | 領取時若該（任務, 嘗試次數）已有未完成的執行，沿用它而不是開新嘗試（T-206 使用） |

### 活動狀態的對應
- 建立有依賴的 PENDING 任務時，該角色中閒置的代理進入 WAITING{upstream}，並記錄在等哪些角色。
- 成功 → COMPLETED（含交接對象）；最終失敗 → FAILED；預算受阻 → WAITING{budget}；可重試的失敗、租約回收、任務取消 → IDLE（新增 `AGENT_IDLE{reason: run_ended}`）。非最終的 `AGENT_RUN_FAILED` / `AGENT_RUN_ABORTED` 仍寫入事件供軌跡使用，但不改變活動狀態。

### 遇到的問題與修正
- Alembic 的自動產生不會比對 CHECK 約束：`lease_iff_running` 改為也要求令牌，必須手寫進遷移 0008。第一版寫法讓命名慣例套用兩次（`ck_tasks_ck_tasks_…`），降版失敗後改為 `op.f()` 搭配裸名稱。
- （我造成的）`succeed` / `fail` 直接把執行推到終止狀態，但執行狀態機要求經過中間狀態。解法是在通用狀態機加入 `shortest_path` / `transition_via`，任務管理員只補完剩餘的合法路徑（每一步都有稽核）。
- 接著資料庫的 `finished_iff_terminal` 又擋下一個真實錯誤：我在走中間狀態之前就設定了 `finished_at`。改為非終止的跳轉先走，`finished_at` 與最後一跳一起寫入。
- 原本設計「預算受阻退還嘗試次數」，但同一（任務, 嘗試次數）的執行資料列已存在，重新領取時違反唯一約束。改為統一「領取即消耗」，並補上 `BLOCKED_BUDGET → FAILED` 的狀態機邊。**教訓：三個錯誤都是資料庫約束先抓到的，證明 T-201 把不變式放在資料庫層是對的。**

### 驗證
- `tests/runtime/test_task_manager.py`：14 個通過，包含驗收條件 **4 個工作程序同時處理 1,000 個任務，每個任務恰好被領取一次、恰好一次執行、全部成功**。
- 全部 Python 測試 **263 個通過**；lint、匯入邊界、`alembic check`（含 0008 降版再升版）、事件結構產生物皆通過。

## 儲存庫重整：frontend/ 與 backend/（D-004，2026-09-18）

### 做了什麼
- 依使用者要求，以 `git mv`（保留歷史）把儲存庫整理為前後端兩個資料夾：
  - `backend/`：單一 Python 專案——`autora/` 套件、`tests/`、`api/`、`worker/`、`scripts/`，以及 `pyproject.toml`、`alembic.ini`；
  - `frontend/`：`web/`（Next.js）與 `event-schema/`（由後端 pydantic 產生、給前端使用的 TS 事件契約）。
- 同步更新：Makefile、CI、三個 Dockerfile（改為整個 `backend/` 複製）、pnpm workspace 與鎖定檔、ruff 設定、README；新增 `.dockerignore`。
- 以路徑定位檔案的程式也一併修正：設定檔尋找 `.env` 與物件儲存預設路徑（`parents[3]`）、API 測試夾具、事件結構產生器的輸出路徑與其測試。
- 單獨一個提交，不與功能變更混在一起。

### 驗證
Python 263 個、TypeScript 17 個測試通過；lint 與匯入邊界、遷移、事件結構檢查、階段 1 驗收皆通過；api、worker、web 三個 Docker 映像檔可建置並啟動。

## T-203 · 工作流程圖（DAG）

### 做了什麼
- `runtime/dag.py`：
  - `WorkflowTemplate` / `NodeSpec`：由領域在程式碼中宣告的工作流程圖（MVP 不讓模型產生流程圖）。註冊時驗證：節點名稱格式、名稱不重複、依賴存在、沒有循環；並計算拓撲順序。
  - `TemplateRegistry`：以名稱登錄範本。
  - `WorkflowEngine`：接上任務管理員的兩個掛鉤，所有傳播都在觸發它的同一個交易內完成。
- `instantiate()`：建立 `workflow_runs` 與每個節點一個任務。先寫 `WORKFLOW_RUN_CREATED` 再寫任務事件，讓前端先知道流程存在；沒有依賴的節點直接就緒。
- 任務成功 → 所有依賴都已成功的下游任務變為就緒，並寫入 `TASK_SUCCEEDED.unlocks`（3D 辦公室用它播放交接動畫）。
- 任務最終失敗或被取消 → 直接與間接依賴它的任務全部取消（它們永遠無法執行）。
- 沒有未完成的任務時關閉流程：全部成功為 SUCCEEDED；有失敗為 FAILED（記錄第一個失敗的任務）；否則為 CANCELLED。
- `cancel()`：人為取消整個流程；新增事件 `WORKFLOW_RUN_CANCELLED`。

### 遇到的問題
- （我造成的）人為取消時，流程關閉原因被記成「任務已取消」而不是操作者給的原因。改為在取消期間暫存原因，關閉時使用。

### 驗證
`tests/runtime/test_dag.py`：18 個通過（循環與缺少依賴的範本被拒、菱形依賴只在兩個上游都成功後才解鎖、失敗的傳遞取消、流程最終狀態）。

---

## T-205 · 政策引擎（遷移 0009）

### 做了什麼
- `runtime/policy/engine.py`：回答「這個行為者可以做這件事嗎」，結果只有三種：允許、需要審批、拒絕。
- 規則由擁有該行為的層級註冊：公司指令在 `company/policy.py`，新聞編輯部的工具在 `domains/newsroom/policy.py`。引擎本身只知道固定原則：

| 原則 | 說明 |
|---|---|
| 人類可以做任何事 | 仍然會被記錄 |
| 未明確允許就拒絕 | 未知的行為、未知的角色、沒有規則，一律拒絕 |
| 不可逆的行為一定要人 | 代理或系統的規則若寫「允許」，會被提升為「需要審批」，任何設定都無法放寬 |
| 公司只能收緊 | `company_policies["policy.overrides"]` 可把某角色的某行為改為拒絕或需要審批；比規則寬鬆的覆寫會被忽略，且決策中會註明 |
| 限額 | 規則可附帶檢查（配額、前置條件），超過時採用規則指定的結果 |

- `decide()` 是純函式；`decide_and_record()` 另外寫入 `policy_decisions` 稽核表（只能新增），拒絕時發出 `POLICY_DENIED`。參數只存雜湊，不存原文。
- 公司預設限額：每週期最多 5 個工作流程；執行長可自行分配的預算上限 5 美元，超過需要人核准。
- **D-001 落地**：系統自動核准文章只在公司政策 `newsroom.auto_approve_if_fact_check_passed` 為真、且事實查核通過時允許；預設需要人核准。

### 驗證
`tests/runtime/policy/test_policy.py`：235 個通過。大部分是把 `platform/07` 的權限矩陣逐格參數化測試，另外涵蓋覆寫只能收緊、不可逆行為必經人、稽核紀錄與事件。

---

## T-206 · 審批（遷移 0010）

### 做了什麼
- `approvals` 資料表：種類、對象（類型 + ID）、關聯的任務與執行、申請者、決定者、原因、到期時間。資料庫保證同一對象同時只能有一個待決的審批；已決定的審批必須有決定者與時間。
- `APPROVAL_FSM`：待決 → 核准 / 駁回 / 過期。
- `runtime/approvals/service.py` 提供三種掛法：

| 掛法 | 情境 | 核准 | 駁回 |
|---|---|---|---|
| 代理執行中 | 政策判定需要審批 | 任務回到就緒，**只有同一個代理**能領回並延續**同一個執行** | 取消任務，下游由工作流程圖一併取消 |
| 人工任務節點 | 任務本身就是決定（例如核准文章） | 任務直接成功，下一個節點解鎖 | 取消 |
| 獨立審批 | 需要核准的指令（例如新專案） | 由發出指令者處理事件 | 同左 |

- 只有人類能做決定；自動核准是政策引擎的決策，不是審批。
- **到期（D-001）**：預設 24 小時。到期只把審批標為過期，任務繼續等待、不會被取消；之後可以為同一件事重新申請。
- 代理執行等待審批時釋放工作程序，代理活動狀態為 WAITING{approval}，而且該代理不能再領其他任務。
- API：`GET /api/approvals?company_id&state`、`POST /api/approvals/{id}/decide`（找不到 404、已決定 409、非人類或參數錯誤 422）。

### 遇到的問題
- （我造成的）取消一個正在等待審批的任務時，會把該執行的代理重複設回閒置（已是閒置又發一次 `AGENT_IDLE`）。改為從「清除等待狀態」的名單中排除該代理。
- 任務管理員補上：領取時優先延續自己等待審批的執行，並排除其他代理暫停中的任務；取消任務時一併中止未完成的執行。

### 驗證
`tests/runtime/test_approvals.py` 9 個、`tests/api/test_approvals_api.py` 4 個通過。

---

## T-208 · Anthropic 模型供應者

### 做了什麼
- `runtime/models/providers/anthropic.py`：把與供應商無關的請求轉成 Messages API（`anthropic` SDK 1.6，beta messages）。
- `runtime/models/factory.py`：依設定建立供應者與閘道（fake 或 anthropic）。
- `count_tokens()`：以相同的請求轉換取得精確輸入權杖數。成本守門員仍使用本地的偏高估算，避免每次呼叫多一次往返。
- 設定新增 `MODEL_PRICES`（JSON，每個模型 ID 的每百萬權杖價格）與 `ANTHROPIC_SERVER_FALLBACKS`。選用 anthropic 時，每個設定的模型 ID 都必須有價格，否則啟動失敗。
- 路由：`frontier` 使用 `FRONTIER_MODEL_ID`，`fast` 使用 `FAST_MODEL_ID`（未設定則同 frontier）；兩者不同時，frontier 的備援為 fast。

### 設計決策（依 Claude API 文件）
| 項目 | 做法 |
|---|---|
| 模型 ID | 程式碼中沒有任何模型 ID，全部來自設定 |
| 思考 | 不傳 `thinking` 參數，使用預設的自適應思考 |
| 思考區塊 | 新增通用型別 `OpaqueBlock`，保存供應商專屬區塊（thinking、fallback 等）的原始內容，下一輪**原封不動**送回；其他供應商的區塊不送出 |
| 拒答 | `stop_reason = refusal` 原樣回報，由代理決定如何處理 |
| 伺服器端備援 | 預設開啟（`fallbacks: "default"`，beta `server-side-fallback-2026-07-01`）；中途切換模型時，`fallback` 標記之前的思考與工具呼叫區塊不送回 |
| 實際服務的模型 | 回報 `response.model`；閘道以**實際服務模型的價格**計算成本（新增 `ModelRouter.price_for`） |
| 結構化輸出 | 以 `output_config.format` 傳送 JSON schema（用 SDK 的 `transform_schema` 轉換） |
| 提示快取 | 自動快取（頂層 `cache_control`），快取讀寫權杖計入用量 |
| 錯誤 | 連線、逾時、限流、5xx、過載 → 允許路由備援；請求錯誤、認證、權限、找不到 → 不備援 |

### 與原計畫不同
- 原本打算用 `beta.messages.parse(output_format=...)`。檢查 SDK 原始碼後發現它在回覆不是合法 JSON 時會直接拋出例外，而拒答與輸出被截斷時正是這種情況。改用 `create` 並自行傳送 schema，驗證仍由閘道負責（不合格時回傳 `output_issues` 給代理修補）。
- SDK 1.6 的傳輸層是 `httpx2` 而不是 `httpx`，測試的模擬傳輸改用 `httpx2.MockTransport`。

### 驗證
- `tests/runtime/models/test_anthropic.py`：29 個通過，不連網、不需金鑰。涵蓋請求與回應對應、思考區塊逐字往返、備援標記之前的區塊省略、拒答、所有停止原因與錯誤對應、設定與路由、依實際模型計價。
- `tests/runtime/models/test_anthropic_live.py`：2 個真實呼叫測試（結構化輸出、兩輪工具迴圈），標記為 `integration`，沒有 `ANTHROPIC_API_KEY` 與 `FRONTIER_MODEL_ID` 時略過。**尚未以真實金鑰執行。**

### 其他
- 執行階段 1 驗收會重新產生前端的事件測試資料檔，只有 ID 與時間不同，這次提交不納入。
- 本機 shell 預設的 Node 是 v12，前端檢查需要使用 nvm 的 v22。

## T-211 · 代理執行器

### 做了什麼
- `runtime/behaviors.py`：`AgentBehavior` 是一個角色的「工作說明」——系統提示、輸出模型（pydantic）、領域驗證器、可用工具、模型能力、每次執行的模型呼叫上限、修補次數上限，以及可選的脈絡與政策事實掛鉤。依角色或（角色, 任務名稱）註冊到 `BehaviorRegistry`。執行環境不知道任何代理的內容，內容由領域（Phase 5 的新聞編輯部）與公司層（執行長）提供。
- `runtime/agent_runner.py`：`AgentRunner.run(claim)` 把一個已領取的任務跑到終點，回傳 `RunOutcome`（completed / failed / aborted / suspended / lost）。它是模型閘道、工具登錄表、政策引擎、審批與活動狀態的唯一呼叫者。

| 階段 | 做的事 | 活動狀態 | 步驟紀錄 |
|---|---|---|---|
| 觀察 | 第一則訊息：任務輸入（加上領域脈絡） | — | — |
| 思考 | 一次模型呼叫，可能帶工具呼叫 | THINKING{plan / reason} | `think` |
| 行動 | 每個工具呼叫先過政策引擎：允許 → 執行；需審批 → 暫停執行、釋放工作程序；拒絕 → 中止 | WORKING{tool}（與 `TOOL_CALLED` 同交易） | `act`（含結果） |
| 評估 | 先由閘道驗證結構，再跑領域驗證器 | REVIEWING{evaluate} | `evaluate` |
| 修補 | 問題清單回饋給模型，再呼叫一次 | REVIEWING{repair} | `repair` |
| 結束 | 交給任務管理員：成功（含交接）、失敗（可重試與否）、中止 | COMPLETED / FAILED / WAITING | — |

### 設計決策
- **交易短而多**：每次活動變更、每個步驟、每個政策決策各一個交易；模型呼叫與工具執行都在交易之外，否則會長時間持有該公司的事件鎖。代價是中途當機會留下部分軌跡，由租約回收器重新排隊、下一次嘗試從頭跑（`platform/03` §5）。
- **每個寫入交易都先做心跳**：心跳會驗證租約令牌。租約被回收後的工作程序無法再寫入任何東西——有測試：工具執行期間租約被回收，之後沒有模型呼叫、沒有步驟、沒有狀態變更。
- **審批後的延續**：工作程序已被釋放（甚至可能重啟過），記憶體裡的對話不在了。因此每個步驟的物件都存 `messages_after`（到那一刻為止的完整對話），延續時從最後一個步驟載入；核准的工具呼叫以審批負載中的 `tool_call_id` 辨識，同一回合中排在它後面的工具呼叫也會接著執行。有測試證明延續後模型看到的對話與原本一致。
- **政策拒絕 → 中止（policy）**，任務最終失敗（`platform/03` §3 的「不通過 → FAILED(policy)」）。模型叫了不存在的工具名稱 → 以錯誤的工具結果回饋，不會當掉；行為宣告了未登錄的工具 → 執行明確失敗（設定錯誤要看得見）。
- **模型拒答 → 不可重試的失敗**（同樣的提示再試一次多半還是拒答）；供應者錯誤與步數用盡 → 可重試的失敗，重試次數由任務管理員控制。
- **修補呼叫不算「思考」**：活動狀態維持 REVIEWING{repair}，步驟種類為 `repair`；步數上限只計算模型呼叫（think + repair）。
- **預算耗盡與驗收條件的差異**：任務拆解的驗收條件寫「ABORTED + FAILED 活動」。T-202 已決定預算受阻的代理是 WAITING{budget}（任務是 BLOCKED_BUDGET，加預算後會被釋放，不是死掉），這裡沿用。為了讓執行軌跡完整，任務管理員在預算中止時另外寫入 `AGENT_RUN_ABORTED{budget}` 作為軌跡資料（不改變活動狀態）。
- 尚未納入：記憶組裝（近期執行摘要，T-609）、活動連結掛鉤、`AGENT_STEP_PROGRESS`（T-304）、從設定組裝執行器（T-213 工作程序主程式）。

### 驗證
`tests/runtime/test_agent_runner.py`：16 個通過，全部使用真實提交的交易（領取在一個交易，執行器開自己的交易），涵蓋：
- 驗收序列 THINKING → WORKING → THINKING → REVIEWING → COMPLETED，含事件負載、步驟種類與序號、成本與權杖累計、步驟物件內容、模型第二次呼叫看到的工具結果；
- 驗證器問題修補一次後通過；閘道的結構問題也會修補；修補用盡 → 任務重試 → 再失敗 → 最終失敗且活動為 FAILED；
- 工具失敗回饋給模型（`TOOL_CALLED` / `TOOL_FAILED` 成對）；未知工具名稱；政策拒絕 → 中止並有 `POLICY_DENIED`；
- 不可逆工具 → 等待人核准 → 同一代理延續同一執行、工具恰好執行一次、對話完整；駁回 → 取消且不再被領取；
- 預算耗盡 → 中止、任務 BLOCKED_BUDGET、活動 WAITING{budget}、模型未被呼叫；供應者錯誤、步數上限、缺少行為定義、租約遺失。

---

## T-213 · 工作程序主程式與 EchoWorkflow（遷移 0011）

### 做了什麼
- `runtime/worker.py`：`Worker` 迴圈。每一輪：
  - **維護**（每 `WORKER_MAINTENANCE_SECONDS`）：回收過期租約、讓逾期審批過期、觸發到期排程；每個工作各自提交，一個失敗不影響其他工作與迴圈；
  - **派工**：對每個「啟用中、角色有行為定義、不是暫停、這個工作程序沒在跑」的代理嘗試領取任務（或延續已核准的執行），以 asyncio 任務執行，最多 `WORKER_CONCURRENCY` 個。
  - 關閉（SIGTERM / SIGINT）：不再領取，進行中的執行有 30 秒寬限，逾時取消；租約過期後由其他工作程序接手，所以硬殺也安全（T-215 證明）。
- `worker/main.py`：程序入口，處理訊號並從設定組裝。
- `app.py`（組裝根）：新增 `build_worker`、`build_behaviors`、`build_tools`、`build_templates`、`load_models`，以及 `simulated_model`（假模型對未寫腳本的請求，依序詢問各領域的模擬）。新增領域只需在這裡註冊。
- `company/agents.py`：`hire_agent`，建立代理時同一交易寫入 `AGENT_CREATED` 與閒置的活動狀態。
- `domains/echo/`：最小但真實的工作流程 `echo.chain_v1`，研究員 → 分析師 → 寫手（使用新聞編輯部的真實角色名稱，讓之後的 3D 辦公室顯示熟悉的座位）。
  - 工具 `echo_note`：寫一筆筆記（新資料表 `echo_notes`），以工具呼叫的冪等鍵唯一，重跑時回傳同一筆；
  - 行為：系統提示、`EchoReport` 輸出模型、驗證器（筆記必須存在且屬於這個任務）、脈絡掛鉤（把上游任務的輸出放進第一則訊息）；
  - 政策：三個角色可以寫筆記；
  - 模擬模型：像真的模型一樣讀提示——先呼叫工具，看到工具結果後回報；`params.pause` 可讓某個任務第一次嘗試的最後回覆變慢（T-215 使用）。
- 新設定：`WORKER_ID`、`WORKER_CONCURRENCY`、`WORKER_POLL_SECONDS`、`WORKER_MAINTENANCE_SECONDS`、`WORKER_COMPANY_IDS`（只跑指定公司，測試與未來分片用）、`TASK_LEASE_SECONDS`、`TASK_RETRY_BASE_SECONDS`。
- docker compose：api 與 worker 共用物件儲存磁碟區（API 讀取工作程序寫入的步驟物件）；worker 的停止寬限 40 秒。
- `scripts/seed_echo.py`：在開發資料庫建立示範公司、進行中的專案與三位代理（可重複執行），供階段 3、4 示範使用。

### 設計決策
- **領域資料表放在領域內**（`domains/echo/models.py`），與 `3d-office/08` 的規劃一致。Alembic 的環境模組屬於資料庫層，不能匯入上層；改由組裝根的 `load_models()` 提供完整清單，並在 `.importlinter` 宣告唯一一條例外（`autora.db.migrations.env -> autora.app`）。
- **事件分派器延後**：規格寫「排程器 + 分派器 + 任務迴圈」。目前沒有任何需要非同步處理事件的處理器（工作流程的傳播在任務交易內完成），做一個空的分派器沒有意義；第一個事件處理器出現時再加。
- **啟動工作流程要求專案為 ACTIVE**（見 T-214）。

### 驗證（階段 2 驗收）
`tests/e2e/test_echo_workflow.py`：以 `build_worker` 組出與正式環境相同的工作程序，在測試中執行：
- 工作流程 SUCCEEDED，三個任務各一次執行、各一筆筆記、共 6 次模型呼叫；
- 每個執行的 `trace(run)` 依序為 TASK_STARTED → AGENT_RUN_STARTED → **THINKING → WORKING → TOOL_CALLED → TOOL_COMPLETED** → THINKING → **REVIEWING** → TASK_SUCCEEDED → **AGENT_RUN_COMPLETED**，步驟為 think / act / think / evaluate；
- 交接：研究 → 分析師、分析 → 寫手、寫作 → 無；
- 每位代理的活動依序變化；**分析師在研究完成前是 WAITING{upstream, 等待 researcher}**，且事件序號證明「分析師等待 < 研究完成 < 分析師開始思考」；
- 分析師的提示包含研究員的回報（脈絡掛鉤）。

另外以開發資料庫做了一次手動煙霧測試：真實的 API 與工作程序兩個程序，以 HTTP 啟動工作流程，約 2 秒內三個任務完成；透過 API 讀到的軌跡與三位代理的狀態都正確；SIGTERM 後工作程序正常結束。

---

## T-214 · 啟動工作流程的 API

### 做了什麼
- `company/workflows.py`：`start_workflow` 指令。檢查專案（存在、屬於該公司、狀態為 ACTIVE）與範本 → 政策引擎判斷並寫入 `policy_decisions` → 實例化。人類操作者與之後的執行長代理（階段 6）走同一個函式，代理會受自己的政策規則限制。
- `POST /api/companies/{id}/workflows`：主體 `{template, project_id, params}`，回傳 201 與工作流程及其任務。

| 狀況 | 回應 |
|---|---|
| 公司或專案不存在、專案屬於其他公司 | 404 |
| 未知範本、缺少參數、專案不是 ACTIVE | 422 |
| 政策不允許（代理） | 403 |
| 未帶或帶錯權杖 | 401 |

### 驗證
`tests/api/test_workflows.py`：7 個通過，包括決策紀錄的行為者是人、上游未完成的代理立即進入 WAITING{upstream}、寫手代理啟動工作流程會被拒絕且留下拒絕紀錄。

---

## T-215 · 當機恢復測試

### 做了什麼
`tests/e2e/test_recovery.py` 以**真實程序**執行（`backend/worker/main.py`、真實租約與時鐘，租約 2 秒）：
1. 工作程序 1 執行研究任務，筆記已提交，接著模擬模型的回覆變慢（120 秒），此時以 **SIGKILL** 殺掉；確認任務仍是 RUNNING、租約仍屬於工作程序 1。
2. 啟動工作程序 2：回收過期租約，研究任務以第 2 次嘗試重跑，接著完成整條鏈；以 SIGTERM 停止，結束碼為 0。
3. **沒有任何東西產生兩次**：三個任務各一筆筆記；第 2 次嘗試的 `echo_note` 透過冪等鍵拿回第 1 次寫的那筆（`TOOL_COMPLETED` 的 `produced` 與摘要「reused note …」可證明）；`TASK_SUCCEEDED` 恰好 3 個；當掉的執行記錄為 ABORTED（`Aborted:timeout`），沒有遺失。

### 需要知道的
- 冪等鍵由（任務、步驟序號、工具名稱、參數）組成，所以重跑時必須在同一步驟以相同參數呼叫才會命中。模擬模型是確定性的；真實模型的參數可能不同，屆時由各領域工具自行決定去重方式（例如新聞編輯部以網址去重證據）。
- 當掉那次執行中途的模型呼叫沒有紀錄，它的成本預留會在 15 分鐘後過期（T-209 的設計）。
- 連續執行 5 次皆通過（每次約 6 ~ 10 秒），目前納入預設測試。

---

## 階段 2 之後的補充：本機運作手冊（2026-09-18）

- 新增並改寫 `logs/RUNBOOK.md`：資料庫、API、工作程序、前端各自的啟動方式與設定；Claude API 串接（模型、價格、備援、驗證方式）；目前以 SQL 調整的代理設定（預算、政策收緊、工具、暫停）；常見問題。手冊中的 SQL 已在開發資料庫以回滾的交易驗證。
- **修正**（我造成的）：真實呼叫測試原本讀取程序環境變數，只寫在 `.env` 的金鑰不會被讀到，測試會安靜地略過，與手冊的說明不符。改為與應用程式一樣讀取設定。
- 新增端到端測試：公司政策把寫手的 `echo_note` 收緊為需要審批 → 寫手停在等待審批 → 人核准後延續同一次執行（手冊中的審批示範）。Python 測試 593 個通過。

## 目前的整體驗證（2026-09-18）

| 檢查項目 | 結果 |
|---|---|
| Python 測試 | **592 個通過**（`AUTORA_REQUIRE_DB=1`，0 略過）；另有 2 個真實呼叫測試預設不執行 |
| TypeScript 測試 | event-schema 8 個、web 9 個 |
| lint 與匯入邊界 | ruff 通過；2 條邊界規則全數遵守（1 條宣告的例外：Alembic 環境 → 組裝根） |
| 資料庫遷移 | 0001 ~ 0011；`alembic check` 無差異；0011 降版再升版成功 |
| 事件結構產生物 | 最新 |
| 階段 1 驗收 | 仍然通過 |
| **階段 2 驗收** | **通過**（`tests/e2e/test_echo_workflow.py`） |
| Docker 映像檔 | worker 可建置 |

## 提交紀錄

| 提交 | 日期 | 內容 | 持續整合 |
|---|---|---|---|
| `30079a8` | 2026-09-18 | 本機運作手冊改寫（資料庫、後端、前端分開運作；AI 代理串接設定）；真實呼叫測試改為讀取 `.env`；審批流程的端到端測試 | ✅ 執行編號 `35304294278` |
| `3def45f` | 2026-09-18 | 新增 `logs/RUNBOOK.md` 本機運作手冊 | — |
| `389b5bf` | 2026-09-18 | T-213 工作程序與 EchoWorkflow、T-214 啟動工作流程 API、T-215 當機恢復（階段 2 驗收通過） | ✅ 執行編號 `35303700478`（python 1 分 28 秒，含子程序當機測試；web 20 秒） |
| `35c05e4` | 2026-09-18 | T-211 代理執行器（Fable 5.1） | ✅ 執行編號 `35302114106`（python 1 分 55 秒、web 27 秒） |
| `d2fa603` | 2026-09-18 | T-203 工作流程圖、T-205 政策引擎、T-206 審批、T-208 Anthropic 供應者 | ✅ 執行編號 `35298973111`（python 1 分 19 秒、web 2 分 3 秒） |
| `5fe458e` | 2026-09-17 | T-202 任務管理員 | ✅ 執行編號 `35226625651` |
| `5afffb7` | 2026-09-17 | 階段 2 第一部分：T-201、T-204、T-207、T-209、T-210、T-212，以及開發紀錄 | ✅ 執行編號 `35221367631`（python 與 web 兩個工作皆通過） |

## 下一步

階段 2 的 15 個任務全部完成，驗收條件通過。進入階段 3 之前還有兩件事：

- **真實 Anthropic 呼叫至少通過一次**（`09_DEVELOPMENT_ROADMAP.md` 的階段 3 進入條件）。需要在 `.env` 設定 `ANTHROPIC_API_KEY`、`FRONTIER_MODEL_ID`、`MODEL_PRICES`，再執行 `pytest backend -m integration`。
- **路線圖列出、但沒有對應任務的兩個 API**：`GET /api/tasks/{id}` 與 `GET /api/runs/{id}`。尚未實作，可在階段 3 的 Dashboard 與 Trace Viewer 需要時補上。

與路線圖寫法不同、已記錄的差異：預算耗盡時代理活動為 WAITING{budget} 而非 FAILED（T-202 / T-211）；排程器不使用 APScheduler（T-212）；S3 物件儲存延後（T-210）；事件分派器延後（T-213）。
