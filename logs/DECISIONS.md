# Decision Log

每筆決策：日期、決定、影響的文件 / tasks、預設值。新決策往下加，不改舊紀錄（被推翻時加一筆新的並註明取代）。

| # | 日期 | 決定 | 影響 | 具體預設 |
|---|---|---|---|---|
| D-001 | 2026-09-16 | **發布審批先由人核准** | `platform/07` Permission Matrix；`3d-office/06` workflow `approve` 節點；T-205 policy defaults；T-514 template | `company_policies.newsroom.auto_approve_if_fact_check_passed = false`。改為自動 = HUMAN action（UpdatePolicy）。Approval 過期 `expires_after = 24h`，過期 → task 保留 WAITING（不取消），下個 cycle snapshot 列出 |
| D-002 | 2026-09-16 | **主語言 zh-TW，en 為第二語言** | `platform/05` §4；T-508 validators；T-509 Writer prompt；公開站預設路由 | `newsroom.primary_lang = "zh-TW"`, `newsroom.langs = ["zh-TW","en"]`, `newsroom.require_all_langs = true`（兩語皆通過才發布；未另行決定前維持）。Writer 先寫 zh-TW 再產 en；claim_ids 兩語相同 |
| D-003 | 2026-09-16 | **web_search 使用 Tavily** | `platform/05` §5 tools；`platform/09`（非模型，屬 tool cost）；T-500 新增 adapter task；T-506 Researcher | tool `web_search` 的 provider = `tavily`；每次呼叫記 `tool_usage`/expense category `tool_cost`；API key 只在 env；fixture 模式下不打外部 API。Tavily 回傳的 `content` 只作候選，**Evidence 仍必須由 `fetch_url` 抓原始頁快照**——search 結果不是 evidence |
| D-004 | 2026-09-18 | **儲存庫以 `frontend/` 與 `backend/` 分層整理** | `3d-office/08` 儲存庫結構；Makefile、CI、Dockerfile、pnpm workspace、ruff 設定 | `backend/`：單一 Python 專案（`autora/` 套件、`tests/`、`api/`、`worker/`、`scripts/`、`pyproject.toml`、`alembic.ini`）；`frontend/`：`web/`（Next.js）、`event-schema/`（產生的 TS 事件契約，由前端使用）；`infra/`、`logs/` 維持在根目錄。取代 `08` 原本的 `apps/` + `packages/` 配置 |
| D-005 | 2026-09-18 | **模型供應者：主要使用 NVIDIA Build 的 `z-ai/glm-5.3`，保留 Anthropic 可隨時切換** | `platform/09` Model Gateway（新增 OpenAI 相容供應者）；T-207 / T-208；`.env` 設定；`09_DEVELOPMENT_ROADMAP` 階段 3 進入條件由「真實 Anthropic 呼叫」改為「真實模型呼叫」 | `MODEL_PROVIDER=nvidia`、`FRONTIER_MODEL_ID=z-ai/glm-5.3`、`FAST_MODEL_ID=z-ai/glm-5.3-flash`、價格 0（免費端點）。切換 = 改 `MODEL_PROVIDER` 與兩個模型 ID；兩把金鑰可同時留在 `.env`。注意：免費端點有速率限制（多數模型每分鐘約 40 次）、試用條款允許 NVIDIA 記錄輸入與輸出、價格為 0 時專案預算無法擋下呼叫；中國團隊模型可能輸出簡體中文，須在提示與驗證器中要求 zh-TW（D-002） |
| D-006 | 2026-09-18 | **主要模型改為 `z-ai/glm-5.3-flash`，暫不設備援**（取代 D-005 的模型選擇；NVIDIA 為供應者、保留 Anthropic 切換不變） | `.env`；`RUNBOOK.md` 第六節；`.env.example` | 實測 NVIDIA 免費端點：`z-ai/glm-5.3` 回答一個字 224 秒（另一次 120 秒無回應），無法供代理使用；`z-ai/glm-5.3-flash` 4 ~ 30 秒，結構化輸出、工具迴圈、EchoWorkflow 全流程皆通過。`FRONTIER_MODEL_ID=z-ai/glm-5.3-flash`、`FAST_MODEL_ID` 留空。glm-5.3 可日後再測或改用付費端點 |
| D-007 | 2026-09-18 | **前端樣式改用 Tailwind CSS v4**（取代 T-309 暫用的 CSS Modules） | `frontend/web`：`postcss.config.mjs`、`src/app/globals.css`；之後所有畫面（T-310 起、階段 4 的 2D 疊層） | 顏色以語意 token 定義在 `@theme`（`bg-surface`、`text-muted`、`border-line`、`bg-accent`…），淺色 / 深色切換只改一組 CSS 變數，不在每個元素寫 `dark:`。不另加元件庫 |
| D-008 | 2026-09-18 | **3D 辦公室人物：免費授權（CC0）的低多邊形人物，風格參考《動物森友會》**；階段 3 先完成再做 3D | `3d-office/04` §10 資產計畫；`12_RISKS` Q4（已回答）、R10；階段 4 的 T-404（`LICENSES.md`） | **只參考風格**：Q 版比例（大頭、短身）、圓潤造型、柔和粉彩、溫馨的辦公室與家具。**不使用任天堂的任何角色、模型、貼圖或商標，也不做可辨識的仿製**。素材只用 CC0（候選：Quaternius、Kenney、KayKit），每個檔案的來源與授權記入 `LICENSES.md`；選定前先確認授權條款。動畫 clip 若素材沒有，自製。**補充（同日）**：辦公室場景另參考任天堂《Good Job!》——明亮色彩、Q 版上班族、辦公桌與隔間的俏皮佈局；同樣只參考風格，不使用其素材或做可辨識的仿製 |
| D-009 | 2026-09-18 | **React 釘在 19.2.x（19.2.8），配合 React Three Fiber 9.7 的支援範圍** | `frontend/web/package.json`；階段 4 全部（T-401 起） | R3F 9.7（目前最新穩定版）宣告 `react >=19 <19.3`，並內建對應 React 19.2 的 reconciler；專案原本是 19.3.0，版本錯配屬於未經測試的組合。改為 `react` / `react-dom` 19.2.8；Next 16.3.5 支援 `^19.0.0`。原有 97 個前端測試、兩個 Playwright 驗收測試皆通過。R3F 發布支援 19.3 的穩定版後再升級（屆時加一筆新決策） |
| D-010 | 2026-09-19 | **辦公室場景改依使用者提供的等角剖面辦公室參考圖**（補充 D-008 的場景部分；人物仍依 D-008） | `3d-office/04` §2、§6、§7；T-402（重做）、T-405 之後的場景相關任務 | 參考圖為商用素材包的渲染圖，**只參考風格，不使用其素材**。風格：正交等角視角、懸浮的剖面房間（白色牆頂與底座邊）、黃色走道、深色亮面工作區地板、白色長條桌配黑色桌腳與雙螢幕、深色辦公椅配彩色條紋、玻璃隔間辦公室與黃色門、會議室、茶水間、休息區、書架與大量植物、柔和陰影、藍灰漸層背景。實作：全部以程式產生的低多邊形幾何（不下載模型），靜態家具合併成一個頂點色網格；木地板與磁磚用程式繪製的材質；一盞從左上方來的主光投射陰影（落向鏡頭側，才看得到）；環境光用 drei `Lightformer`（不下載 HDR）；色調映射用 Neutral（保留原色） |
| D-011 | 2026-09-19 | **辦公室外觀提供五套室內設計風格，預設「日式無印」；辦公室有入口、接待區與各區標示；人物頭部縮小**（依使用者回饋，補充 D-010 的配色部分） | `office3d/palette.ts`（`THEMES`）、`scene/{layout,furniture,Floors,Labels,textures}`、`OfficeCanvas`、`theme.ts`；T-413 | 風格（使用者指定）：**日式無印**（淺色櫸木、白牆、亞麻與淺灰，沉穩）、**侘寂風**（暖色灰泥牆、舊木、陶土與苔色，低對比柔光）、**工業風**（清水泥、黑色鋼構與鋼框玻璃、回收木桌、干邑色皮沙發、磚紅點綴）、**Google 風**（軟體公司辦公室：白與淺木，各區用鮮明的原色地毯、繽紛家具；只借用配色風格，不用任何標誌或品牌素材）、**電光風**（賽博龐克，當作夜間模式：暗色地板與走道上發光的網格、偏藍弱光、電光色霓虹用滿整個色階——外圍牆頂亮藍、玻璃框青、門框螢光綠、各區地毯外圍各一種螢光色邊線〔研究青、編輯紫、行銷綠、彈性座位黃、接待橙、茶水間紅〕、桌緣發光；霓虹部分不受光照直接發光，另成一個網格）。光線（半球光、主光、環境光強度、窗外光）也跟著風格走。切換鈕在 3D 畫面左上角，選擇只記在這個瀏覽器（localStorage），不送伺服器。**不做逐項自訂顏色**（使用者說太複雜就先不要）。角色的顏色（椅背條紋、衣服）各風格都一樣，用來辨認角色。入口在右側前走道盡頭；審批桌兼接待櫃台，與休息區合為接待區。人物頭部骨頭在程式裡縮為 0.8 倍（不改素材檔）。D-010 的原始配色（黃走道、深色工作區）不再提供 |
| D-012 | 2026-09-19 | **Embedding 用 NVIDIA Build 的 `nvidia/nemotron-3-embed-1b`（2048 維），向量存成 pgvector 的 `halfvec(2048)` + HNSW 索引**（回答 Q-embed） | `runtime/models/embeddings.py`（`embed` binding）、`domains/newsroom/models.py`（`evidence_chunks`）、`.env`（`EMBED_PROVIDER`、`EMBED_MODEL_ID`）；T-503 起的證據搜尋 | 沿用現有 NVIDIA 金鑰（使用者同意的建議）。2026-09-19 實測：NVIDIA 帳號可用的 embedding 模型中它可用、跨中英檢索有效（中文問句對英文相關段落 0.51、中文相關段落 0.58、無關段落 −0.01），只提供 2048 維（`dimensions` 參數只接受 2048）；pgvector 的 `vector` 型別索引上限 2000 維，所以用 `halfvec`（上限 4000，儲存減半）。模型是設定、不寫在程式；換模型時維度必須相同，否則要新 migration（`EMBED_DIM`），每個段落記錄是哪個模型算的，搜尋只比對同一模型的向量。模擬 / 測試用確定性的雜湊向量（`EMBED_PROVIDER=fake`）。embedding 呼叫記在 `model_calls`（alias `embed`），價格來自 `MODEL_PRICES`（沒列就是免費） |
| D-013 | 2026-09-19 | **工作流程引擎加入「服務步驟」與「宣告式迴圈」；核准的決定可由領域掛鉤處理；任務狀態機新增服務步驟的 READY → SUCCEEDED / FAILED**（補充 T-203 的靜態 DAG；範本仍由程式宣告，LLM 不產生 DAG） | `runtime/dag.py`（`NodeSpec.service`、`Loop`、`WORKFLOW_RUN_EXTENDED`）、`runtime/services.py`（`ServiceRegistry`、`ServiceDispatcher`，工作程序每輪執行）、`runtime/approvals`（`on_decided`）、`runtime/lifecycles.py`、`TaskManager.link_hooks`；T-514 起的新聞室流程 | **服務步驟**：沒有代理執行的節點（核准、發布）由註冊的處理函式在任務的交易裡決定——完成、等人核准、或失敗；處理函式拋錯或沒有決定都算失敗（錯誤記在任務輸出）。服務步驟沒有 run 也沒有租約，所以任務可直接 READY → SUCCEEDED / FAILED（`RUNNING` 必須持有租約，資料庫有約束）。**迴圈**：範本宣告 `Loop(check, back_to, again, carry, max_rounds)`；`check` 成功且 `again(輸出)` 為真時，引擎新增 `back_to` … `check` 一輪任務（`carry(輸出)` 併入新一輪第一個任務的參數），原本等 `check` 的任務改等新一輪；超過 `max_rounds` 再要求時取消等待中的任務（工作流程以 CANCELLED 結束）。**核准掛鉤**：`ApprovalService.on_decided(action, hook)`，人做決定時在同一個交易裡執行（例：核准或駁回文章），掛鉤失敗則決定不成立。**活動連結**：各領域的 `activity_links(task, run)` 掛在任務管理器上，代理工作中與完成時寫進 `agent_activity.detail.links`（失敗不影響任務，在 savepoint 裡執行） |
| D-014 | 2026-09-19 | **發布後的量測不另排 +1h / +24h / +7d 的工作，改由每小時的讀者統計涵蓋**（調整 platform/05 §2 流程圖的 `measure` 節點與 §6 的 ARTICLE_PUBLISHED 處理） | `domains/newsroom/analytics.py`、`publisher.py`；T-516；Phase 6 的 CompanySnapshot（T-603） | 公司第一次發布時建立每小時的排程 `newsroom.collect_analytics`（每小時第 7 分）；每次重算昨天與今天（UTC）的 `analytics_daily`（每篇、每語言、每天：瀏覽、不重複讀者、讀完），重算而非累加，重複或延遲執行結果相同；數字有變才發 ANALYTICS_DAILY_UPDATED。+1h / +24h / +7d 的數字可從每日資料與發布時間得到（24 小時與 7 天以日計），不需要每篇文章三個排程；若 CEO 報告需要小時級的「發布後 1 小時」再加。原始 beacon 30 天後刪除（platform/08） |
| D-015 | 2026-09-20 | **公開站的網址移到 `/news` 底下**（`/news/{lang}`、`/news/{lang}/articles/{slug}`；原本是 `/{lang}/...`） | `domains/newsroom/publisher.article_path`（所有公開網址的唯一來源：站台發布紀錄、社群草稿、文章頁的 `public_urls`、`ARTICLE_PUBLISHED.url`）、`frontend/web/src/app/(site)/news/[lang]/**`；`platform/05` §4、`3d-office/06`、`08` | 使用者決定：新聞內容統一歸類在 `/news` 下，根網址留給之後的其他用途（公司首頁等）。所有公開路徑都由 `article_path` 產生，改一個函式即可；已發布文章的 `distributions.copy` 裡是當時寫下的網址快照（示範資料才有舊路徑，不回填） |
| D-016 | 2026-09-20 | **組織架構修訂 v2：公司 / 事業 / 部門 / 職務 / 代理分離**（`ARCHITECTURE_V2.md`）。CEO 只管公司（資本、事業組合、優先順序），內容決策交給 Editor-in-Chief；Newsroom 從「等於公司」降為「AI Media 事業的一個部門」 | 新增 `business_units`、`departments`、`roles`、`products` 四張表；`agents` 加 `department_id`/`role_id`；`projects`/`budgets`/`transactions`/`company_goals` 加事業歸屬；3D 辦公室改為可進入的部門空間 | **`role` 字串維持不變**，執行層（behaviors、`required_role`、policy、router）一行不改——router 的路由鍵是 `role.capability` 且只容許一個點，所以角色不能命名空間化，組織關係放在 `roles.department_id`。**推翻**階段 4 的「空間依角色分區、同一層樓、不切換頁面」與 D-011 寫死六區的部分（前提已不成立）。**必需品**：`AGENT_CREATED` 要帶部門，且需新增 `AGENT_ASSIGNED` 事件，否則 reducer 契約會壞、avatar 永遠不會換房間。`platform/10` 規劃中的人類 RBAC `roles` 表改名 `member_roles` 以免撞名 |
| D-017 | 2026-09-20 | **架構修訂 v2.1：機會 → 提案 → 事業的商業迴圈**（`ARCHITECTURE_V2_1.md`）。`opportunities`、`opportunity_signals`、`business_proposals` 為獨立實體；探索與驗證**就是 Project**；`customers` 與 `transactions.customer_id` 定義好但延後實作 | 新增 T-611（機會與提案）、T-612（客戶，延後）；T-603 加 KPI 掛鉤、T-604 加八個機會/事業命令、T-605a 加機會評估、T-610 加產業中立測試 | **不新增迴圈**：機會與提案有狀態機但沒有排程器，只在每日 cycle 的階段裡前進，一輪最多一步。探索與驗證做成 Project，於是預算、成本歸屬、停損全部沿用既有機制（`projects` 只加一個可空的 `opportunity_id`）。**人只在投資決策、收掉事業、超過門檻的資本配置介入**；探索與驗證便宜可逆，不需人審，核准的對象是**那份凍結的提案**。發現一處真實耦合：`company/reporting_min.py` 的 `PUBLISHED_EVENT = "ARTICLE_PUBLISHED"` 讓 Core 認得新聞室詞彙（import-linter 攔不到字串），改由領域註冊 KPI 掛鉤 |
| D-018 | 2026-09-20 | **`customers` 提前實作**（取代 D-017「等真的有收入再做」的延後）。理由不是「現在需要它」——現在仍然沒有收入——而是它是 §7 七種切法裡唯一缺的那一塊，而且是一張表加一個可空欄位；等第一塊錢進來的那天才補，補的是 migration 加回填 | migration 0032；`company/customers.py`；`Ledger.record(customer_id=...)`；Reporting 的 `customers` 指標（company 與 business unit 兩個 scope） | **不存個資**：只有 `external_ref`（金流商的 id）與 kind，沒有姓名、email、卡號——有測試斷言欄位集合，多一個可疑欄位就紅。**刻意不做**：發票、稅務、應收應付、對帳、多幣別換算（與 D-017 一致）。churn 不刪列：賺過的錢仍然發生過 |
| D-019 | 2026-09-21 | **移除 `companies.type`**（不是改成自由字串，是整欄拿掉）。v2 之後公司是一個**事業組合**，「做哪個產業」屬於 `business_units`——同時經營 AI Media 與 AI Education 的公司沒有單一 type。證據：沒有任何程式分支讀它、前端從不顯示它、資料庫裡每一列都是 `newsroom`（含產業中立測試建立、根本沒有新聞室的公司） | migration 0034；`CompanyType` enum、`create_company(type=...)`、`CompanyOut.type`、`CompanyCreate.type` 全部移除；OpenAPI 與前端型別重新產生 | **事件 `COMPANY_CREATED.type` 保留但改為可空且不再寫入**：已經記下的事件帶著它，讀的人必須分得出「沒有」與「錯的」。資料不保留（全部相同且都不為真）。日後若需要機器可讀的產業標籤，加在 `business_units` 上，而且它的意思由那門生意定義（§9）。這也讓 `test_core_vocabulary` 的 `KNOWN_LEAKS` 歸零：**領域以下每一層都不再出現任何領域詞彙** |
| D-020 | 2026-09-21 | **治理的「連續失敗自動暫停」只數代理自己的失敗**。供應商逾時、租約過期、預算用盡、任務管理器中止——這些不說明代理能不能做它的工作，不計入連續次數（**跳過，不算成功**：中間發生幾次故障都不影響，它自己連續失敗三次照樣被暫停） | `company/governance.py` 的 `NOT_THE_AGENT_S_FAULT` 與 `_runs_that_judge()`；四個新測試 | 發現方式：2026-09-21 第一次真實模型 soak——免費端點連續三次 504，**治理把公司的 CEO 停職**，剩下三輪全部 fallback。「代理壞了」與「端點掛了」要做的決定相反，而原本的規則分不出來。前綴比對（中止會帶原因，如 `Aborted:timeout`）。**沒有改的**：人仍然可以手動暫停任何代理；供應商故障仍然記在 `agent_runs.error` 與 `model_calls` 裡，只是不再被當成代理的過錯 |

| D-021 | 2026-09-22 | **領域接線是組裝根的明示工作，不做動態領域註冊**（取代 AC-S5 最後那句「刪除 `domains/newsroom` 後底層測試全綠」，該句經實驗證實不成立）。`autora.app` 逐一點名每個領域；`register(runtime)` 只負責「執行期才接得上」的部分（服務、審批回呼、任務掛鉤、階段掛鉤），其餘（models metadata、事件目錄、policy、templates、tools、KPI、排程、設定）在 Runtime 存在之前就要接，由組裝根直接 import | `11_MVP_ACCEPTANCE.md` AC-S5 改寫；`autora/domains/__init__.py` 的 docstring 改成描述實際做法（原文宣稱每個子套件都有 `register(runtime)`，echo 沒有）；不動任何程式接線 | 實驗（2026-09-22，尾-2）：刪掉 `domains/newsroom` 後 runtime 有 154 個測試照樣通過，其餘失敗全部來自三個**正確**行為——遷移的 `env.py` 經 `load_models()` 看所有資料表、`tests/company` 的 conftest 串到含新聞室 router 的 API app、產生給前端的事件型別是所有領域事件的聯集。**沒有一處是底層 import 了領域**。<br>**為什麼不現在做動態註冊**：只有一個領域時，它買到的只是讓一句文件成真；代價是註冊順序要另立規則、接錯從 import 期延到執行期、「現在到底接了什麼」不能再用 grep 回答。**選項沒有關掉**：能不能改成註冊表，取決於領域知識有沒有散到組裝根以外，而這件事由 `lint-imports` 每次 CI 守著。等第二個領域（事業迴圈開出新事業）要進來時再做，那時「刪掉一個領域公司還能開」才是有人真的要用的性質 |
| D-022 | 2026-09-22 | **第一條營收模型：付費訂閱，金流商用 Stripe；發布通路維持只用自有網站**（回答 `16` 的 P1 第 7 題與 P0 第 1 題 / Q-channel）。使用者決定。T-701 的範圍依此收斂：只做訂閱要用的**價格、訂閱、付款**三張表 | migration 0035（`prices`、`subscriptions`、`payments`）；`company/subscriptions.py`；`Ledger.record(product_id=...)`；事件 `SUBSCRIPTION_STARTED`、`SUBSCRIPTION_STATE_CHANGED`、`PAYMENT_RECEIVED`；`14_TASK_BREAKDOWN` T-701 / T-703 / T-704 的註記 | **放在 `company/`，不是 `domains/business/`**：訂閱與付款每個產業都一樣（`test_core_vocabulary` 照樣全綠），跟 `customers`（D-018）同一層。**金流商是 token 不是分支**：`provider = 'stripe'` 只是字串，核心沒有任何 `if stripe`；Stripe 的 SDK、簽章驗證、事件對應全部留給 T-702 的 adapter。**不存個資**延續 D-018：三張表都只有金流商的 id 與金額，有測試掃欄位名稱。**付款 append-only**（與 `transactions` 同一個 trigger），一筆付款對一筆帳本 revenue，冪等鍵 `payment:{provider}:{external_ref}`。**訂閱是承諾、付款才是錢**：訂閱變 ACTIVE 不入帳。取消客戶最後一個訂閱 = 客戶流失（呼叫 `customers.churn`）。**刻意不做**：`leads`（訂閱站的 lead 就是 email，違反不存個資）、`orders`（沒有一次性商品）、`campaigns` / `campaign_spend`（只用自有網站、沒有付費廣告；T-703 延到真的要買廣告時）；退款、proration、優惠券、稅務（等 webhook 真的收到再各自成為一個函式）。`products` 與 `opportunities` 在 T-600 / T-611 已經做了。**之後的通路**：使用者希望之後串 Instagram、Threads 等社群，每週固定時間產出——記為 T-704 的方向（`DistributionChannel` adapter + 每週排程），開做時另立決策 |
| D-023 | 2026-09-22 | **公司的錢以新台幣為主幣別；計量維持美元，結算時以固定匯率換算**（回答 `16` 的 P1 第 10 題）。起因：台灣的金流只收新台幣，帳本卻是美元，兩種幣別混在一起加總，餘額就是錯的。使用者決定以新台幣為主，換算方式照建議 | `infra/money.py`（`Fx`、`format_money`）；`BASE_CURRENCY`、`FX_RATES` 設定；migration 0036（`transactions.source_amount` / `source_currency` / `fx_rate`、四張表預設幣別改 TWD、既有資料一次換算）；`Ledger.record`、`settle_cycle`；Reporting、Dashboard KPI、CompanySnapshot、cycles API；成本守門員；policy 門檻；CEO 提示 | **兩種錢分開**：**計量**（`model_calls.cost_usd`、預留、每次執行／每個任務的上限、Tavily 費用）維持美元，因為供應商就是用美元收費，換算只會多一層誤差；**公司的錢**（帳本、預算、收入、報表、CEO 看到的數字、審批門檻）全部是新台幣。**帳本只有一種幣別**：`Ledger.record` 是唯一入口，非主幣別的金額在這裡換算，原始金額和匯率記在同一列，事後可以對帳、可以重現；之後改匯率不會改寫過去。**匯率先用設定檔的固定值** `FX_RATES={"USD":"32"}`，手動更新；缺匯率的幣別直接拒絕，不會用猜的。**守門員**把新台幣預算換成美元再比較（預算是一個數字、花費是很多筆，所以移動預算）。**指標改名**：`cost_usd`／`revenue_usd`／`profit_usd`／`model_cost_usd` → `cost`／`revenue`／`profit`／`model_cost`（單位就是主幣別），`views_per_usd` → `views_per_cost_unit`；policy 的 `governance.*_limit_usd` 拿掉 `_usd`。**預設值照 32 換算**：CEO 撥款門檻 160、探索上限 64、擴大事業 1600、模擬預設撥款 160。**既有資料**在 migration 裡以 32 一次換算（交易保留原始美元與匯率、預算、KPI 快照、停損條件、目標、policy），可以完整降級；**不換算**：計量表、付款與價格（記的是金流商收的幣別）、凍結的提案、cycle 的計畫與覆盤文件（那天寫下的內容）。畫面：公司的錢在 zh-TW 語系顯示為「$87.50」，計量成本維持「US$」 |
| D-024 | 2026-09-22 | **收款改為統一金流（PAYUNi）單次付款，買一年使用權；讀者用 Email 登入**（取代 D-022 的「Stripe 定期訂閱」；D-022 的通路決定與 T-701 範圍的其餘部分不變）。起因：台灣的銀行帳戶不支援 Stripe；綠界審核麻煩；統一金流個人會員可以申請。使用者決定不做定期扣款：「只要做一次收款，然後給對方一年的使用權」 | 下一批：migration 0037（訂閱改為使用權）、`company/subscriptions.py`、讀者帳號與登入、T-702 改為統一金流單次收款 | **先以個人身分收款**，營運超過一定金額再轉公司（個人會員信用卡額度每月 20 萬、撥款 T+7）。**單次收款不用申請幕後授權與信用卡 Token**，信用卡、ATM、超商代碼都能收。**使用權**：付款日起一年；到期前續購從原到期日往後延一年，到期後才買從付款日起算；狀態只剩 ACTIVE → EXPIRED，時間到自動到期；到期沒續購 = 客戶流失。**收入在收款當天全額入帳**（現金制；分攤到 12 個月之後需要再做）。**讀者 Email 登入**：寄登入連結、不用密碼；email 只存在網站自己的讀者表，公司層與代理讀不到——D-018「代理讀得到的地方沒有個資」的原則不變，但「整個系統不存個資」不再成立，改為「個資只在讀者表」 |
| D-025 | 2026-09-23 | **付費牆採「大部分免費、部分會員專屬」；年費 NT$360；登入信用 Resend 寄；讀者帳號自成一層 `autora.accounts`** | `autora/accounts/**`（`readers`、`login_tokens`、`reader_sessions`，migration 0038）；`infra/email`（Resend 與 console 兩種寄件者）；`/api/auth/*`；新聞室文章的 `access` 欄位；公開站的登入頁與會員專屬提示；`.importlinter` 新增一條「沒有人可以 import 讀者」 | 使用者決定：文章預設免費，個別文章可以標成會員專屬；年費 NT$360（之後調價不影響已購買的人）；寄信用 Resend。**讀者帳號自成一層**：`autora.accounts` 放在 `domains` 同層（app 之下），並用 import-linter 禁止 `company`、`runtime`、`db`、`domains` import 它——**代理與公司層讀不到任何讀者的 email**，這是 D-018 那條界線在有登入之後的樣子。公司層看到的只有 `customers.external_ref = "reader:<id>"`。**登入不用密碼**：輸入 email → 寄一次性連結（15 分鐘、用過即失效、資料庫只存雜湊）→ 建立工作階段 cookie。**不透露誰是會員**：要求登入連結時一律回同樣的答案，不管這個 email 有沒有帳號。**寄信是可換的**：`EMAIL_PROVIDER=console` 時印在終端機（開發與測試不打外部 API），`resend` 才真的送 |
| D-026 | 2026-09-23 | **2D 房間的美術改由 3D 場景烘焙產生：以正交俯角把每件 3D 物件離線渲染成像素精靈圖，canvas2D 合成器照舊貼圖**（取代目前手繪的精靈圖 DSL 美術；合成器、深度排序、走動代理、跑馬燈不變） | `office3d/fallback/art/**`（`atlas.ts` 的手繪精靈改為烘焙出的圖檔）、新的烘焙腳本與產出的圖資；`3d-office/04` 的 2D 章節 | 使用者回饋：手繪的點陣房間「風格與理想的落差很大」，並提議直接用 3D 的正視俯角出圖。**採用他的點子，但用在產圖而不是即時渲染**——2D 同時是 WebGL 的退路（`chooseMode()` 在 `no-webgl2` 或視窗太窄時強制走 2D），若 2D 變成「同一個 three.js 場景換正交相機」，沒有 WebGL 的瀏覽器就沒有東西可看，手機也要扛整個 3D 場景。另外正交相機直接拍出來是低多邊形等距模型、不是像素畫，要到附圖的味道還需要渲染到低解析度 render target 再以 nearest 放大（可選色階量化）——這道流程放在烘焙腳本裡，只跑一次。**因此**：每件家具／人物以固定的等距角度離線渲染成精靈圖存成圖檔，執行期的 canvas2D 只負責貼圖，樓層配置仍是資料驅動，沒有 WebGL 也能畫。代價：新增家具要重烘一次（腳本化），且圖檔會進版本庫 |
| D-027 | 2026-09-23 | **2D 烘焙的做法定案：烘「材質槽＋明暗階」而不是顏色，跟著 3D 的五種風格換色；相機 60°、每公尺 32 像素；外框與投影在烘焙時算出**（補充 D-026） | `frontend/web/tools/bake-sprites/`（`render.ts` 三道渲染、`bake.mjs`、`preview.ts` 對照表）；`scene/furniture.ts` 的 `BAKEABLE`；`fallback/art/baked.ts`（產生的檔案，進版本庫）、`fallback/art/theme.ts`；對照表存在 `logs/3d-office/assets/d027-*.png` | 使用者要求 2D「最好可以隨著3D切換風格跟著變化」，並說他手動抓的俯視角度不準、請我修正。**跟著風格變**：每件家具烘三遍——不打光、每個調色盤槽塗成專屬探針色（得到「這個像素是什麼材質」）；只打光、全白（得到「多亮」，量化成 5 階）；沿主光壓扁到地面（得到投影）。字元代表（材質槽, 明暗階），執行期拿當前主題的調色盤查表上色——換風格只是換一張對照表，不重烘、不存五份圖。明暗階帶一點色相偏移（暗部偏冷、亮部偏暖），像像素畫師手做的色階。**外框**用 sel-out：外框像素取相鄰材質的最暗階，木頭配深棕、葉子配深綠；有 `glow` 的主題（電光風）外框改用建築的霓虹邊色，否則深色家具在深色地板上看不見。**投影**沿與明暗同一個主光方向把幾何壓到地面，所以影子形狀和 3D 一致。**角度與解析度從對照表選**（`pnpm -F web bake-sprites --preview`）：45° 椅子幾乎只剩椅腳、75° 椅子變成一個方塊、60° 兩者之間；16 px/m 椅子只有 10 像素、角色色條紋只有 2 像素，認不出來，32 px/m 書架上的書看得出來。**不需要影像生成模型**：精靈圖是用專案自己的 three.js 幾何算出來的，每次結果相同、跟 3D 永遠一致。**已知缺口**：沒有 CI 檢查「改了 3D 家具但沒重烘」——烘焙需要瀏覽器與 WebGL，macOS 本機與 CI 的 SwiftShader 是否逐像素一致沒有把握，寧可不做會誤報的檢查；目前只有測試檢查兩邊的家具名單一致 |
| D-028 | 2026-09-23 | **2D 的人物也從 3D 烘焙：每個代理穿他在 3D 的那個角色；顏色取自角色貼圖、不跟風格換；每個角色限 10 色**（補充 D-027） | `tools/bake-sprites/people.ts`、`art/baked-people.ts`（產生的檔案）、`art/people.ts`；`agents/body.ts`（`AVATAR_SCALE`、`SEAT_LIFT`、`STAND_BACK` 移出 `AgentAvatar`，3D 與烘焙共用）；2D 看板改為動態載入；對照表 `logs/3d-office/assets/d028-*.png` | 使用者：「人物烘焙開始」。原本 2D 所有人都是同一個手畫小人、臉朝鏡頭，坐在朝另一邊的桌前。**現在**：12 個 Kenney 角色（CC0）照 3D 的比例、縮小的頭（0.8）、坐姿抬高、同樣的動作片段擺姿勢後烘成像素圖——坐著（背影，面向螢幕）、走路（朝鏡頭／背向／側面各 4 格，左側面為右側面鏡像）、站立（完成時站在椅子後面）；每個代理穿 `roster` 給的角色，跟 3D 同一個。**顏色**：人物的顏色來自角色共用的貼圖，不是風格調色盤——3D 裡人物在五種風格都一樣（D-011），2D 也一樣；鍵的「槽」直接是顏色 `#rrggbb`，外框照樣吃電光風的霓虹邊。**10 色上限**：貼圖有 255 色、一個角色看得到二十多色，乘上 5 階明暗會超過鍵的字元數，也不像像素畫；以中位切割（每組取最常見的**真實**貼圖色，不平均出新色）減到 10 色，而且對**同一角色的所有姿勢一起算**，走路時顏色不會一格一格跳。**影子**：人物用腳下的小橢圓，不投影身體（腿在走路中間的投影在這個尺寸只是雜訊）；坐著的沒有自己的影子，椅子的就是。**大小**：人物資料 518 KB（gzip 75 KB），所以 2D 看板改成**只在顯示 2D 時才載入**——正式建置驗證過，辦公室頁一開始的 HTML 裡沒有這個檔案，只在動態載入清單裡 |
| D-029 | 2026-09-23 | **2D 地板的紋理在「用某個風格上色」時才鋪，不烘進圖裡；圖樣照 3D 的 `textures.ts`、以公尺計**（補充 D-027） | `fallback/art/floorPattern.ts`（新）；`floorPainter.ts` 畫背景時呼叫；對照 `logs/3d-office/assets/d029-*.png` | 使用者：「地板紋理開始」。**不能烘**：地板用哪種材質、多大，是風格的一部分——開放區在日式無印是 1.2 公尺的木地板、工業風是 1.6 公尺的水泥板、電光風是發光網格；一次烘焙對所有風格共用，裝不下。**做法**：背景用某個風格上色後，逐個像素看烘焙記下的槽——是地板的（`floors.<區>.color`，以及該風格沒有霓虹邊線時退成地板的邊線）才鋪圖樣，牆、窗、前緣、外框都不動；每個地板像素知道自己在辦公室的哪裡（一公尺橫 32、縱 28 像素），所以接縫落在跟 3D 一樣的位置。圖樣照 3D：木地板每塊四排、端縫錯開；磁磚二乘二、用 3D 的灰縫色；石板二乘二、亮縫、塊與塊深淺略異；地毯方塊加細點；電光風每公尺一條發光線、中線較淡。**像素畫的規矩**：接縫一個像素，每塊木板一個平色，細點一個像素，沒有漸層。**木地板接縫比 3D 淡**：一排木板只有 8 像素深，照 3D（64 像素裡 2 像素）的深淺畫成 1/8 會變成磚牆 |
| D-030 | 2026-09-23 | **2D 看板在 3D 辦公室閒置時就先在背景下載**（取代 D-028 裡「只看 3D 的人不下載 2D」的部分；2D 仍是延後載入的獨立 chunk） | `OfficeCanvas.tsx`：`preloadBoard2D`、`whenIdle`、`preload` 測試接口；`office-canvas.test.tsx` 四個測試 | 使用者決定：「要，背景預先載入 2D」。起因：修終端機模式 e2e 的工作階段量測到，這個 React 版本的導航遇到還在載入的延後元件會**等它**，而不是先顯示「載入中」，所以 3D 啟動中切到 2D 要等 2D 下載完。**做法**：畫面是 3D 時，等頁面空閒（`requestIdleCallback`，最多等 4 秒；沒有這個 API 的瀏覽器改用 1.5 秒延遲）再 `import()` 同一個模組——跟延後載入的看板是同一個 chunk，之後切到 2D 直接從快取拿。畫面本來就是 2D 時不做（已經在載入）；同一次造訪只做一次；空閒前離開頁面就取消。**代價**：只看 3D 的人也會在載入後下載一次（人物與家具約 75 KB gzip）。**驗證**：正式建置裡人物資料只在一個 chunk（沒有重複一份），而且不在辦公室頁一開始的 HTML 裡 |
| D-031 | 2026-09-24 | **財務代理（T-705）的提案就是一道 `AllocateBudget` 指令，財務角色送出一律等人審；它唯一的工具是 `submit_command`；每期在 MEASURING、報表寫完之後問它，MEASURING 等它審完才結束** | `company/agents/finance.py`（新：行為、四個驗證器、脈絡）、`company/finance.py`（新：`FinanceDesk`、範本 `company.budget_review_v1`）；`policy.py`：`submit_command` 允許 finance；`organization.py`：行政部門多一個「CFO」職位；`app.py` 接線；模擬財務長 | 使用者：「做 T-705 財務代理」。驗收是「沒有任何寫 transactions 的路徑」。**不另做一張提案表**：原本的指令管線已經有「送出 → 政策判斷 → 開審批 → 核准後照做」，政策也早就寫著 `needs_approval("allocate_budget", "finance")`；另開一張表只會多一條要守的路。**鎖在三個地方**：(1) 工具只有 `submit_command`；(2) 政策對財務角色除了這個工具以外沒有任何「允許」——`record_transaction` 沒有規則所以拒絕，`payment` 是不可逆動作、引擎一律要人；(3) 核准後寫的是 `budgets`（未來花費的上限），不是 `transactions`，帳本只由帳本自己寫。**第二道鎖在驗證器**：這一輪送出的 `AllocateBudget` 若不是「等待審批」就退回——萬一哪天有人把政策放寬，會在這裡被看到。**引用的數字要對得上報表**：每個提案附上依據（指標、範圍、數值），逐一和這一期存下的 KPI 比對，對不上就退回。**時間點**：MEASURING 進場時報表先寫，再開財務審查；到了 REVIEWING，提案已經在審批收件匣裡等人，CEO 的快照也把它們算進待審數量。沒有聘財務代理的公司照舊，MEASURING 立刻結束。 |
| D-032 | 2026-09-24 | **商業代理（T-706）只負責「發現」：每週看一次市場，用兩道新指令記下機會與訊號，不經人審；它引用的每個數字與網頁在寫入當下就檢查**（T-706 原文寫「→ HUMAN」，依 ARCHITECTURE_V2_1 §6 改為發現不審、人把關開事業） | `company/agents/business.py`（新）、`company/market_watch.py`（新：`MarketWatchDesk`、範本 `company.market_watch_v1`、「Market watch」專案）；`verbs_business.py`：`DiscoverOpportunity`、`RecordOpportunitySignal`；`policy.py`：兩個新動作只允許 business；`app.py`：`RESEARCH_TOOLS` 把新聞室的 `web_search`／`fetch_url`／`read_evidence` 借給 business；`reporting.figure_issue`（從財務代理抽出共用）；行政部門多一個「Business Development」職位 | 使用者：「做 T-706 Business 代理」，並選了兩個選項：**照 v2.1 自己記錄**（不是每個新機會都等人審）、**公司數據 + 網路搜尋**。**為什麼是「發現」**：T-611 已經有 CEO 評估機會、策略師寫提案、人核准開事業；`opportunities.discover` 與 `add_signal` 存在，但正式程式碼裡沒有任何呼叫者，測試裡的機會都是手動塞的。**權限**：business 角色只被允許 `submit_command`、兩道新指令、三個借來的閱讀工具；打分、推進、否決、寫提案、配預算、開事業一律沒有。**寫入時檢查，不是事後檢查**：`source: company` 的數字要等於最近一期報表（Decimal 比較，可指定事業）；`source: web` 必須引用這一輪任務的工具**自己產生**的東西（`TOOL_COMPLETED` 的 `produced`）；代理不能用 `source: person`；一次最多新增 3 個機會；已決定的機會不能再加訊號、同 key 不能重新發現。人不受這些檢查（紀錄會寫是誰）。**時間點**：EXECUTING 進場時，若有在職的 business 代理、而且 7 天內（容許 12 小時誤差）沒有未失敗的觀察，就開一次；費用記在「Market watch」專案，kill criteria 與探索相同，超過就暫停、要有人恢復才會再看。 |
| D-033 | 2026-09-24 | **快照的 `last_cycle` 是「最近一個已經量測過的 cycle」，不是「最近一個已結束的 cycle」**：報表在 MEASURING 寫完之後，當天這一期（還沒結束）就是 `last_cycle` | `company/snapshot.py` 的 `_last_cycle`；`tests/company/test_snapshot.py`、`tests/acceptance/test_revenue.py` | T-708 驗收時發現：財務長在 MEASURING、CEO 在 REVIEWING 拿到的快照裡，`last_cycle` 是**前一期**，而且 KPI 是空的——因為它只在「最新一筆公司 KPI 屬於那一期」時才附數字，而最新一筆永遠是今天的。所以兩個代理審的是「今天」，看到的公司層級數字卻是空白（各事業的數字在 `portfolio` 裡有）。第一天連期數都沒有。**改法**：最新一筆公司 KPI 屬於還沒結束的那一期時，就描述那一期（期數、階段、失敗的任務、KPI；它的 review 還沒寫，所以是空的）。隔天 PLANNING 時，那一期已結束、最新 KPI 也是它的，結果和以前一樣——所以 PLANNING 與備援計畫的行為不變。 |

## 尚未決定的 P0

| # | 問題 | 暫用預設（未決定前照此實作） |
|---|---|---|
| Q-embed | Embedding 供應商 | `embed` alias 抽象化（T-503）；MVP 先以任一可用的 OpenAI-compatible embedding endpoint 實作 adapter，換供應商只改設定。**（2026-09-19 已由 D-012 決定）** |
| Q-budget | 每日預算數量級 | `company.daily_cap_usd = 10`, `max_workflows_per_cycle = 5`, per-run 上限見 `platform/04`。**（D-023 之後預算是新台幣：10 美元 = NT$320）** |
| Q-channel | 發布通路是否僅自有網站 | 僅自有網站；Marketing MVP 只產文案草稿。**（2026-09-22 已由 D-022 決定：維持自有網站；之後的外部通路方向是 Instagram / Threads 每週固定時間產出）** |
