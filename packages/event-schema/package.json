{
  "name": "@autora/event-schema",
  "version": "0.0.1",
  "private": true,
  "description": "Generated zod schemas + TS types for Autora events. Source of truth: autora.runtime.events (pydantic). src/generated.ts and schema/ are generated by `make gen-schema`; do not edit them.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./catalog.json": "./schema/events.catalog.json"
  },
  "scripts": {
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "zod": "^4.6.5"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "typescript": "^5.9.0",
    "vitest": "^5.0.0"
  }
}
